<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Homepage</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <nav>
    <div class="nav-login">
      <% if (locals.user) {%>
        <h3>Hey, <%= user.firstname %></h3>
        <a href="/create-message">Create message</a>
        <a href="/logout">Logout</a>
      <% } else { %>
        <a href="/login">Login</a>
        <a href="/sign-up">Sign Up</a>
      <%}%>
    </div>
  </nav>

  <h1>Random thoughts from you Klubhouse legends:</h1>
  <p>To see who and when people wrote messages, login or sign up!</p>
  <p>It costs you nothing. Not even your freedom. Just do it.</p>
  <% if (locals.user) {%>
    <h1>WELCOME BACK <%= user.firstname %></h1>
  <% } else { %>
    <h1>please log in</h1>
    <div class="card">
      <% locals.messages.forEach( (message) => { %>
        <div class="message">
          <h3><%= message.title %></h3>
          <p><%= message.message %> </p>
          <p>By: <%= message.author %></p>
        </div>
      <% }) %>
    </div>
  <%}%>
</body>
</html>
